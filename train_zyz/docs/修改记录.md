# 修改记录

## 2026-05-13 (v13)：显存 / 通讯 / 性能指标全面优化

### 背景

针对 Qwen-2.5-72B 在 80GB A100 上训练，目标：
1. 降低 GPU 显存占用，力争 72B 从 3 卡降到 2 卡
2. 优化 FSDP NCCL 通讯效率
3. 加入全面性能诊断指标（每步耗时分解、显存结构分解、分卡查看）

### 修改

**Phase 1 — FSDP CPU Offload（核心显存优化）**

- `sft_train.py` 新增 `--cpu_offload`（默认 False）：`CPUOffload(offload_params=False)` 将 AdamW 优化器状态（momentum+variance, fp32）移至 CPU RAM
- `sft_train.py` 新增 `--backward_prefetch`（默认 "pre"）：FSDP `BackwardPrefetch.BACKWARD_POST` 延迟预取，进一步节省显存
- `sft_train.py` 新增 `--fused_optimizer`（默认 True）：`AdamW(fused=True)` 单 CUDA kernel 优化器 step，5-10% 加速
- 显存分析：72B FULL_SHARD 所需显存从 ~1152/N GB 降至 ~288/N GB（CPU offload 开启时）
- 蓝图新增 `CpuOffload`（bool）和 `BackwardPrefetch`（Literal["pre","post"]）参数
- `submit_sft.py` 新增 `CPU_OFFLOAD` 和 `BWD_PREFETCH` 配置项

**Phase 2 — NCCL 通讯优化**

- `sft_train.py` Python 层：`NCCL_ALGO=Ring`、`NCCL_PROTO=Simple`、`NCCL_MIN_NCHANNELS=4`
- 蓝图 shell 层：追加 `NCCL_NSOCKS_PERTHREAD=4`、`NCCL_SOCKET_NTHREADS=2`
- LoRA 蓝图同步 NCCL 环境变量

**Phase 3 — DataLoader 优化**

- `prefetch_factor=4`、`persistent_workers=(num_workers > 0)` — 减少 worker 重启开销
- 诊断频率降低：step 进度 10→50、内存诊断 40→100、慢步阈值 30s→20s

**Phase 4 — 全面性能指标系统**

- **CUDATimer** 类：基于 `torch.cuda.Event(enable_timing=True)` 精确 GPU 计时
  - 指标：`perf.forward_ms`、`perf.backward_ms`、`perf.optimizer_ms`、`perf.comm_ms`、`perf.total_ms`、`perf.tokens_per_sec`
  - 写入频率：每 `perf_metric_steps`（默认 50）step，仅 rank 0
- **get_memory_breakdown(device_id)**：`allocated_gb`、`reserved_gb`、`peak_allocated_gb`、`fragmentation_pct`
  - 写入频率：每 `memory_metric_steps`（默认 20）step，**所有 rank**（带 `labels={"rank": "N"}` 支持 Magnus 面板分卡过滤）
- **estimate_memory_components(model, n_gpu)**：step=0 打印理论显存估算（参数/优化器/梯度/合计）
- 新增 CLI：`--perf_metric_steps`（默认 50）、`--memory_metric_steps`（默认 20）

### 效果

- 72B 模型 + 2×80GB A100 可训练（开启 `--cpu_offload` + `--backward_prefetch post`）
- Magnus 面板可按 GPU 分别查看显存占用，快速定位显存瓶颈
- 每步前向/反向/优化器/通讯耗时独立可见，判断性能瓶颈来源

### 修改文件

- `train/sft_train.py` — +CUDATimer, +get_memory_breakdown, +estimate_memory_components, +5 CLI args, FSDP cpu_offload/backward_prefetch, fused AdamW, NCCL setdefault, DataLoader tuning, 训练循环埋点
- `train/blueprints/OpenFundus_SFT_zyz.magnus` — +NCCL env vars, +CpuOffload/BackwardPrefetch 参数
- `train/blueprints/LoRA_zyz.magnus` — +NCCL env vars
- `train/submit_sft.py` — +CPU_OFFLOAD / +BWD_PREFETCH 配置项
- `PHY-LLM-Basic-Algorithm/sft_train.py` — 同步副本

---

## 2026-05-06 (v10)：run_generation_eval NCCL 超时修复 — barrier 同步防止 rank 脱节

### 问题

训练日志中出现 NCCL `_ALLGATHER_BASE` 超时（600s），发生在 step=0 的初始生成式评估（`[推理-initial]`）期间。崩溃链：

```
run_generation_eval() 被调用
  → rank 1: if local_rank != 0 → return（立即退出函数）
  → rank 1: 进入训练循环 → FSDP forward → _ALLGATHER_BASE 等待 rank 0
  → rank 0: 仍在 run_generation_eval() 中逐样本生成（~10 min / 101 条）
  → rank 0: 不参与训练循环的 collective
  → 600s 后 watchdog 杀 rank 1 → SIGABRT
```

根本原因：`run_generation_eval()` 中 `if local_rank != 0: return` 让非 rank 0 进程在 state dict 收集（NCCL collective）之前就返回，然后立即进入训练循环发送需要 rank 0 参与的 FSDP allgather。但 rank 0 还在生成文本（~10 分钟），无人参与该 collective。

### 修改

**sft_train.py** `run_generation_eval()` 重构同步流程：

1. **State dict 收集移到 `local_rank` 检查之前**：所有 rank 参与 `model.state_dict()` 的 NCCL collective
2. **barrier 1**：state dict 收集后同步，确保 NCCL 操作全部完成
3. **非 rank 0 等待 barrier 2**：阻止非 rank 0 进程立即返回训练循环
4. **barrier 2**：rank 0 推理完成后通知所有 rank，同步返回训练循环

```python
# 旧：rank 1 立即返回 → 进入训练循环 → NCCL 超时
if local_rank != 0:
    return

# 新：所有 rank 先参与 state dict collective，barrier 同步后再分流
_state = model.state_dict()        # all ranks
dist.barrier()                     # sync after collective
if local_rank != 0:
    dist.barrier()                 # wait for rank 0 to finish generation
    return
# ... rank 0 generates ...
dist.barrier()                     # signal completion
```

### 效果

- 生成式评估期间不再有 NCCL 超时
- 所有 rank 在推理完成后同步进入训练循环
- FSDP forward 的 allgather 始终有全部 rank 参与

### 修改文件

- `train/sft_train.py` — `run_generation_eval()` 重构 barrier 同步
- `PHY-LLM-Basic-Algorithm/sft_train.py` — 同步副本

---

## 2026-05-06 (v12)：精简输出 + save_steps 100 + 蓝图 NCCL NVLink 修复

### 问题

1. 每 40 步 checkpoint 做一次生成式评估（101 条样本），产生大量中间 `eval_results_step_*.json`，训练变慢且无实际意义
2. `save_steps=40` 对 7B 模型过于频繁
3. 蓝图 `NCCL_P2P_DISABLE=1` 在 v1 用于规避 IB 问题，对单节点 NVLink 反而严重拖慢通信（6 卡步时 ~20s）
4. 最终评估 JSON 只有 `model_output` 和 `gt_output` 两个原始文本，缺少结构化的答案/解答拆分

### 修改

**训练流程精简**：
- `--save_steps` 默认值: 40 → 100
- 移除 checkpoint 时的 `run_generation_eval()`，仅保留 loss 评估 + 权重保存
- `train_log` 新增 save_steps 节点：每个存档点同时记录 train_loss + eval_loss（之前 eval_loss 只在 epoch 结束时记录）
- 仅在训练全部结束后执行**一次**完整测试集生成式评估 (`tag="final"`)

**最终评估 JSON 增强**（`eval_results_final.json` / `eval_results.json`）：
- 新增 `parse_answer_solution()` 函数，按 "答案：...\\n解答：..." 格式自动拆分
- 每条样本含 7 个字段：

```json
{
  "id": 0,
  "question": "...",
  "gt_full": "答案：...\\n\\n解答：...",
  "gt_answer": "...",
  "gt_solution": "...",
  "model_full": "答案：...\\n\\n解答：...",
  "model_answer": "...",
  "model_solution": "..."
}
```

**蓝图 NCCL 修复**（SFT + LoRA）：
- `NCCL_P2P_DISABLE=1` → 移除（改为 `NCCL_P2P_LEVEL=NVL`）
- `NCCL_IB_TIMEOUT=22` → 移除（IB 已禁用，无作用）
- `NCCL_NET_GDR_LEVEL=0` → 移除（同上）
- `NCCL_DEBUG=INFO` → `WARN`（减少 NCCL 日志噪音）

### 效果

- 训练输出简化为 2 个核心 JSON：`training_log.json`（loss 曲线）+ `eval_results_final.json`（完整问答）
- 6 卡步时预期从 ~21s 降至 3-6s
- `plot_training.py` 可直接使用 `training_log.json`（含完整的 train/eval loss 数据点）
- 最终评估 JSON 可直接用于人工审阅：题目、模型答案、模型解答过程、标准答案、示例解答

### 修改文件

- `train/sft_train.py` — save_steps 100 + 移除 checkpoint eval + parse_answer_solution + JSON 增强
- `train/OpenFundus_SFT_zyz.magnus` — NCCL P2P NVL + 移除冗余 env
- `train/LoRA_zyz.magnus` — 同上
- `PHY-LLM-Basic-Algorithm/sft_train.py` — 同步副本

---

## 2026-05-06 (v11)：SFT 性能优化 — 硬件加速 + 7B/72B 差异化 + NCCL 修复

### 修改

**sft_train.py** 全局优化：

1. **A100 硬件加速**
   - `torch.set_float32_matmul_precision('high')` — TF32 tensor core
   - `torch.backends.cudnn.benchmark = True` — 固定 shape 自寻最优算法
   - `config.attn_implementation = "flash_attention_2"` — 显存节省 30%+，推理/训练加速（需 flash_attn 已安装；无则 fallback sdpa）

2. **NCCL 单节点优化**
   - `NCCL_P2P_LEVEL=NVL` + `NCCL_P2P_DISABLE=0` — 撤销蓝图的 P2P_DISABLE，走 NVLink 直连（同节点双 A100）
   - `NCCL_IB_DISABLE=1` 保留 — 单节点不需要 IB
   - `NCCL_SOCKET_IFNAME=^docker,lo,virbr` — 排除虚拟网卡

3. **FSDP 弃用警告抑制**
   - `warnings.filterwarnings` 过滤 `torch.distributed.fsdp` 的 FutureWarning
   - **不迁移到 DCP `get_state_dict`**：新 API 不支持 `rank0_only`，72B 模型会导致每 rank 各持一份完整 CPU state dict（~144GB × N），必然 OOM

4. **7B/72B 自动差异化配置**
   - `is_large_model = total_params > 30`（>30B → 大模型模式）
   - 7B：跳过 `gradient_checkpointing_enable()`，显存充足无需以时间换空间
   - 72B：保持开启 `gradient_checkpointing_enable()`，显存安全优先

5. **训练前初始评估轻量化**
   - 注释 `run_generation_eval(tag="initial")` — 训练前生成式评估耗时 10+ 分钟，收益有限
   - step=0 loss 评估保留（train_loss + eval_loss），提供训练前 baseline
   - checkpoint 评估 + final 评估完整保留，训练全周期质量变化仍可追踪

6. **DataLoader 多线程 + 锁页内存**
   - `num_workers: 0 → 2`，`pin_memory=True`，`prefetch_factor=2`
   - CPU→GPU 传输加速，tokenization 并行化

**submit_sft.py** 默认值修正：
- `MAX_LENGTH: 4096 → 1024` — 与 sft_train.py argparse default 一致，防止序列长度意外翻倍导致显存暴涨

### 拒绝的 AI 建议

- **`torch.compile`**：与 FSDP 兼容性不稳定，可能引入 hang/OOM，暂不启用
- **DCP `get_state_dict` 迁移**：新 API 不支持 `rank0_only`，72B 模型每 rank 各持 ~144GB CPU state dict，必然 OOM
- **`NCCL_TIMEOUT` 环境变量**：NCCL 无此环境变量，无效
- **`init_process_group(timeout=7200)`**：根因已由 v10 barrier 修复，无需延长超时

### 修改文件

- `train/sft_train.py` — 性能优化 + 7B/72B 差异化 + 初始评估移除
- `train/submit_sft.py` — MAX_LENGTH 4096→1024
- `train/eval_baseline.py` — [新建] 训练前基线评估独立程序
- `train/OpenFundus_SFT_zyz.magnus` — `_on_exit` trap 统一上传所有 eval JSON + training_log.json
- `train/LoRA_zyz.magnus` — 同上
- `PHY-LLM-Basic-Algorithm/sft_train.py` — 同步副本

---

## 2026-05-06 (v7)：蓝图 eval_custody 上传 v6 生成式评估文件 + download_results.py

### 问题

v6 新增的 `run_generation_eval()` 生成的 `eval_results_initial.json`、`eval_results_final.json` 未被上传到 Magnus，训练完成后用户无法下载。蓝图 `eval_custody` 只上传了旧的 `eval_results.json`（eval-only 推理结果）。

### 修改

**OpenFundus_SFT_zyz.magnus** `eval_custody` 重写：
- 改为遍历 `eval/` 下的 3 个文件并逐个上传：`eval_results.json`, `eval_results_initial.json`, `eval_results_final.json`
- 文件不存在时跳过（打印 `# xxx 不存在，跳过`）
- 每个文件打印独立的 `magnus receive` 下载命令

**OpenFundus_SFT_zyz.magnus** `result_section` 增强：
- 新增 `gen_eval_initial` / `gen_eval_final` 字段到 `$MAGNUS_RESULT`
- 统计生成式评估的样本数

**train/download_results.py（新建）**：
- 从 Magnus 任务日志中提取所有 `magnus receive` 命令及其 secret
- 自动下载所有找到的文件到指定目录
- 用法：`python download_results.py <job_id> [--output ./results/]`

### 效果

- 未来训练任务完成后可下载完整的 3 份评估文件
- download_results.py 一键下载，无需手动从日志复制 secret
- 对当前已完成的作业：文件在集群 `/data/magnus/models/.../eval/` 下，需直接访问集群文件系统获取

### 修改文件

- `train/OpenFundus_SFT_zyz.magnus` — eval_custody 重写 + result_section 增强
- `train/download_results.py` — [新建] 下载助手脚本
- `PHY-LLM-Basic-Algorithm/sft_train.py` — 同步副本

### 附加修复：MASTER_PORT 随机化防 EADDRINUSE

**问题**：`MASTER_PORT=29501` 和 `--rdzv_endpoint=localhost:29501` 硬编码，同节点端口被占用后所有重试确定性 EADDRINUSE 失败。

**修复**（两个蓝图 SFT + LoRA）：
- 初始端口：`export MASTER_PORT=$((RANDOM % 20000 + 30000))`
- 重试循环内每次重新生成随机端口
- 最终兜底也重新生成
- `--rdzv_endpoint=localhost:$MASTER_PORT` 使用变量

## 2026-05-06 (v9)：移除 sft_train.py base64 传递 — 使用 Magnus 内置 GitHub 仓库检出

### 问题

蓝图通过 base64 编码将 `sft_train.py`（~40KB）作为参数传递，在 entry_command 中解码写入 `/tmp/sft_train.py`。这是完全多余的：

1. **Magnus 平台自动 clone GitHub 仓库**到 `$MAGNUS_HOME/workspace/repository/`（也是容器 `--pwd`），`sft_train.py` 天然就在 `./sft_train.py`
2. `repo_name = "OpenFundus"` 但 `sft_train.py` 在 `PHY-LLM-Basic-Algorithm` 仓库，仓库名不匹配
3. `github_path` 蓝图参数从未实际使用（始终走 base64 路径）
4. base64 编码/解码增加约 50% 体积开销，污染蓝图参数空间

### 修改

**OpenFundus_SFT_zyz.magnus**（瘦身）：
- 删除 `GithubPath`、`SftTrainB64` 参数类型定义
- 删除 `github_path`、`sft_train_b64` 从 `blueprint()` 签名
- 删除 entry_command 中的 base64 解码 block（~14 行 Python heredoc）
- 所有 `/tmp/sft_train.py` → `./sft_train.py`（Magnus checkout 后直接可用）
- `repo_name`: `"OpenFundus"` → `"PHY-LLM-Basic-Algorithm"`（匹配实际仓库）

**submit_sft.py**（简化）：
- `_build_bp_args()` 移除 `sft_train_b64` 编码（~8 行）和 `github_path` 参数
- GitHub 同步步骤保留（仍需 git push 让 Magnus 检出最新版本）

### 效果

- 蓝图瘦身 ~20 行（base64 block + 参数定义）
- 不再有 40KB base64 字符串污染蓝图参数
- Magnus UI 的 GitHub Repository/Branch 选择器可直接使用
- `sft_train.py` 更新流程：本地修改 → `submit_sft.py` 自动 git push → Magnus 自动 checkout 最新版

### 修改文件

- `train/OpenFundus_SFT_zyz.magnus` — 删除 GithubPath/SftTrainB64 参数 + base64 解码 block，修正 repo_name
- `train/submit_sft.py` — `_build_bp_args()` 移除 sft_train_b64/github_path
- `修改记录.md` — 新增 v9 条目

## 2026-05-06 (v8)：提示词前缀优化 + base64 沙箱修复 + inspect_storage 重写

### 提示词前缀三轮优化

**第一轮**：根据训练集格式（`答案：...\n\n解答：...`）设置初始默认提示词。

**第二轮**：查看完整训练样本后细化：
- 新增多问题目处理 `(1)...; (2)...` 格式
- 答案与解答之间显式空白行分隔
- 要求写明定理/公式/变换方法
- 强调关键推导步骤不可省略

**最终版**（[submit_sft.py:85-94](train/submit_sft.py#L85-L94)）：
```python
PROMPT_PREFIX = (
    '你是一位数学解题专家。请逐步推理并解答以下问题。\n'
    '\n'
    '输出格式：\n'
    '答案：[最终结果。多问时用 (1)...; (2)... 分别列出。数学公式使用 LaTeX $...$]\n'
    '\n'
    '解答：[完整推导过程。写明所用的定理、公式或变换方法，关键推导步骤不可省略]\n'
    '\n'
    '{instruction}'
)
```

### 蓝图 `import base64` 沙箱限制修复

**问题**：`OpenFundus_SFT_zyz.magnus` 在 `blueprint()` 函数中 `import base64` 对 `prompt_prefix` 编码，但 Magnus 蓝图沙箱禁止导入 `base64`，launch 时报错：
```
Runtime Error in Blueprint: Import of 'base64' is not allowed in Blueprint
```

**修复**：
- **蓝图**（[OpenFundus_SFT_zyz.magnus](train/OpenFundus_SFT_zyz.magnus)）：
  - 新增 `PromptPrefixB64` 隐藏参数（base64 预编码字段）
  - 删除 `import base64` 块，改为直接检查 `prompt_prefix_b64` 透传到 shell
- **submit_sft.py**（[submit_sft.py:196-200](train/submit_sft.py#L196-L200)）：
  - `PROMPT_PREFIX` 非空时预计算 `base64.b64encode()` → 传入 `prompt_prefix_b64`

**数据流**：`submit_sft.py (base64 编码) → blueprint (透传，无 import) → shell $PROMPT_ARG → sft_train.py (base64 解码)`

### inspect_storage.py 重写：存储检查 → 训练数据样本查询

**之前**：遍历 `/data` `/tmp` 目录树展示文件布局。

**现在**（[inspect_storage.py](train/inspect_storage.py)）：
- 读取训练数据文件（`.json` / `.jsonl` / `.parquet`），展示完整样本（不截断）
- 全局统计：样本数、字段列表、各字段 avg/min/max 长度
- 均匀采样（含首尾各一条）
- 新增 `system_entry_command` 挂载 `/data:/data`（修复容器内 `/data` 为空的问题）
- 新增 `--samples` `--data-path` CLI 参数

**用法**：
```bash
python inspect_storage.py                      # 默认 3 条
python inspect_storage.py --samples 5          # 每文件 5 条
python inspect_storage.py --data-path /custom/path/train.json
```

### 修改文件

- `train/submit_sft.py` — 提示词优化 + base64 预编码
- `train/OpenFundus_SFT_zyz.magnus` — 新增 PromptPrefixB64 参数，移除 import base64
- `train/inspect_storage.py` — 重写为训练数据样本查询工具
- `修改记录.md` — 新增 v8 条目
- `train/SFT.md` — 同步更新 inspect_storage 说明 + prompt_prefix 文档

## 2026-05-06 (v6)：sft_train.py 加入训练三阶段测试集生成式评估 + JSON 采集

### 问题

训练过程中只记录 eval_loss（单一标量），缺少对测试集的详细生成式评估，
无法对比模型在训练前、训练中、训练后的实际生成质量变化。

### 修改

**train/sft_train.py** 新增 `run_generation_eval()` 函数：
- FSDP 兼容：rank 0 收集完整 state dict → 创建临时推理模型 → 逐样本生成 → 清理
- 非 FSDP 模式直接用已有模型推理
- 对测试集每条样本使用 `model.generate()` 生成 response
- 保存为 `eval/eval_results_{tag}.json`，含 question / gt_output / model_output 字段

`train()` 中三处调用点：
1. **Step=0 初始 loss 后** → `tag="initial"`，训练开始前的模型能力基线
2. **每个 save_steps checkpoint 后** → `tag="step_{global_step}"`，训练过程的能力变化
3. **save_final 最终模型保存后** → `tag="final"`，训练结束后的最终能力

### 效果

- 训练前/中/后三阶段可直接对比模型生成质量变化
- JSON 文件可用于人工评测或自动化指标计算
- FSDP 下临时模型推理不影响训练状态（gather → 推理 → 释放 → 恢复训练）

### 修改文件

- `train/sft_train.py` — 新增 `run_generation_eval()`，train() 三处调用
- `PHY-LLM-Basic-Algorithm/sft_train.py` — 同步副本

## 2026-05-04 (v5)：重构 SFT 蓝图 — 提取 sft_train.py + GitHub 拉取

### 问题

蓝图 `OpenFundus_SFT_zyz.magnus` 内嵌了全部 Python 训练/评估代码（~700 行字符串），导致：
- 蓝图臃肿，混合 Python 字符串和 shell 代码难以维护
- 修改训练逻辑必须编辑 .magnus 文件并重新上传到 Magnus
- 无法独立测试训练脚本 / 版本化管理

### 修改

**train/sft_train.py（新建）**：从蓝图 SFT_TRAIN_PY + _EVAL_PY 提取为独立文件。
- 所有训练函数（train, evaluate, save_checkpoint, save_final, SFTDataset 等）
- 新增 `--eval-only` 模式用于独立推理
- 新增 `--save_steps`、`--retry_seed` 参数
- argparse CLI 接口，所有超参数通过命令行传入

**OpenFundus_SFT_zyz.magnus（瘦身）**：
- 删除 SFT_TRAIN_PY（~650 行）和 _EVAL_PY（~80 行）
- 新增 `github_path` 参数（默认 `PHY-LLM-Basic-Algorithm/sft_train.py`）
- 训练脚本改为 `wget` 从 `raw.githubusercontent.com/Rise-AGI/{github_path}` 拉取
- 评估改为 `sft_train.py --eval-only` 模式

**train/submit_sft.py（增强）**：
- 保存蓝图到 Magnus 后，同步 sft_train.py 到 GitHub（git add/commit/push）
- 等待 10s 让 GitHub CDN 传播后再启动任务
- 同步失败时警告但继续（离线 fallback）

**PHY-LLM-Basic-Algorithm/sft_train.py**：同步到仓库根目录。

### 效果

- 蓝图瘦身 90%（从 ~1320 行到 ~560 行）
- 训练代码可以独立测试、版本化管理
- 修改训练逻辑只需更新 GitHub，无需重新上传蓝图
- 支持本地直接运行 `python sft_train.py --model_path ...`

### 修改文件

- `train/sft_train.py` — [新建] 独立训练/评估脚本
- `OpenFundus_SFT_zyz.magnus` — 删除 SFT_TRAIN_PY/_EVAL_PY，添加 github_path，Python urllib 拉取（替代 wget，容器内无 wget）
- `submit_sft.py` — 添加 GitHub 同步步骤
- `SFT.md` §1 — 添加 sft_train.py 说明 + 架构图
- `PHY-LLM-Basic-Algorithm/sft_train.py` — [新建] 同步到仓库根目录

## 2026-05-04 (v4)：training_log 加入 step=0 初始 loss

### 修改

两个蓝图 `OpenFundus_SFT_zyz.magnus` 和 `LoRA_zyz.magnus` 的训练循环前加入 step=0 的初始 loss 记录：

- **train_loss**：取第一个 batch 做 float32 前向计算（多卡时 all_reduce 取平均）
- **eval_loss**：完整 eval 集前向计算（复用已有的 `evaluate` 函数）
- 打印到日志并写入 `training_log.json`，`global_step=0`，`epoch=0.0`

### 效果

- `plot_training.py` 绘制的 Loss 曲线包含初始点，能直观看到模型在训练开始前的 baseline
- training_log.json 的 step 级别记录从 step=0 开始（之前从 step=logging_steps 开始）
- 不影响 resume 逻辑（`train_log` 每次启动重建，step=0 是当前 run 的初始状态）

### 修改文件

- `OpenFundus_SFT_zyz.magnus` — `train()` 函数中数据预检后、训练循环前插入
- `LoRA_zyz.magnus` — 同上位置

## 2026-05-04 (v3)：Checkpoint 损坏恢复修复 — 原子保存 + 损坏检测 + 最终兜底

### 问题

训练中途被杀（OOM / NCCL 超时 / 节点抢占）导致 checkpoint 保存不完整，重试时 `_load_safetensors_state` 因 `SafetensorError` 崩溃，且无 try/except 兜底。Shell retry 每次恢复同一损坏 checkpoint，确定性失败。3 次重试耗尽后直接退出，没有从头开始的选项。

崩溃链：
```
训练被杀 → save_pretrained 写到一半
  → checkpoint-latest/model.safetensors 截断
    → 重试时 _load_safetensors_state 抛异常
      → Step 4 无 try/except → 崩溃
        → Shell retry 恢复同一损坏 checkpoint × 3
          → FATAL 退出
```

### 修改

**两个蓝图** `OpenFundus_SFT_zyz.magnus` 和 `LoRA_zyz.magnus`：

#### 问题 A — 原子保存（防损坏产生）
```python
# 旧：直接写 checkpoint-latest，中途被杀 → 截断
m.save_pretrained("checkpoint-latest", state_dict=state)

# 新：写临时目录 → os.rename 原子覆盖
tmp_path = f".ckpt-tmp-{step}"
m.save_pretrained(tmp_path, state_dict=state)
os.rename(tmp_path, "checkpoint-latest")   # 原子操作
```
被杀时 `.ckpt-tmp-XXX` 损坏不影响已有 checkpoint。

#### 问题 B — 加载容错（损坏时优雅降级）
```python
# 旧：直接 load_file，损坏文件抛 SafetensorError → 进程崩溃
state = load_file(single_path)

# 新：try/except 包裹，返回 None
try:
    state = load_file(single_path)
except Exception as e:
    log(f"checkpoint 损坏: {e}")
    return None

# Step 4：try/except 包裹，None 时从头训练
try:
    state = _load_safetensors_state(ckpt)
    if state and len(state) > 0:
        model.load_state_dict(state, strict=False)
    else:
        log("无有效权重，从头开始训练")
except Exception as e:
    log(f"加载失败: {e}，从头开始训练")
```

#### 问题 C — Shell 最终兜底（防确定性失败）
```bash
# 3 次重试耗尽后 → 清理损坏 checkpoint → 再尝试最后一次
if [ $TRAIN_OK -eq 0 ]; then
    rm -rf "{output_dir}/checkpoint-latest"
    torchrun ... --resume_from_checkpoint "" ...
fi
```

### 效果
- 原子保存消除 checkpoint 损坏的主要来源
- Python 层容错让单次损坏不会导致进程崩溃
- Shell 最终兜底确保即使所有重试失败也不会直接退出

### 修改文件
- `OpenFundus_SFT_zyz.magnus` — `save_checkpoint`、`_load_safetensors_state`、Step 4 resume、shell retry
- `LoRA_zyz.magnus` — `save_lora_checkpoint`、`_load_safetensors_state`、shell retry
- `SFT.md` §8.13 — 新增 checkpoint 损坏处理文档
- `LoRA.md` §6.9 — 同步更新

## 2026-05-03 (v2)：NaN loss 处理修复 — float32 loss + 计算图安全的 NaN 跳过

### 问题

训练到 step ~110 时产生 NaN loss，NaN 处理代码用 `torch.zeros_like(loss)` 替换损失，但该操作破坏了计算图（`zeros_like` 返回叶子张量，无 `grad_fn`），导致 `backward()` 失败。由于 FSDP 依赖 backward hook 触发 NCCL REDUCE_SCATTER 同步，backward 失败后 NCCL collective 卡死，10 分钟后 watchdog 杀进程。

崩溃链：
```
样本产生 NaN loss
  → zeros_like(loss) 返回叶子张量，无 grad_fn
    → backward() 报 "does not require grad"
      → FSDP 的 REDUCE_SCATTER 未执行
        → 后续 ALLGATHER/REDUCE_SCATTER 死等前面的完成
          → NCCL watchdog 10min 超时 → SIGABRT
```

### 修改

**两个蓝图** `OpenFundus_SFT_zyz.magnus` 和 `LoRA_zyz.magnus` 的训练循环：

#### 问题 A — float32 loss 计算（防 NaN 产生）
```python
# 旧：bfloat16 下 HuggingFace 内部 loss，大词表(151k)数值下溢出
outputs = model(input_ids=input_ids, attention_mask=attn_mask, labels=labels)
loss = outputs.loss

# 新：手动 float32 cross_entropy
outputs = model(input_ids=input_ids, attention_mask=attn_mask)
logits = outputs.logits
shift_logits = logits[..., :-1, :].contiguous().float()   # ← 关键：转 float32
shift_labels = labels[..., 1:].contiguous()
loss = F.cross_entropy(shift_logits.view(-1, logits.size(-1)), shift_labels.view(-1), ignore_index=-100)
```

#### 问题 B — 计算图安全的 NaN 跳过（防 NCCL 死锁）
```python
# 旧：zeros_like 破坏计算图 → backward 失败 → NCCL 死锁
loss = torch.zeros_like(loss)          # leaf tensor, no grad_fn
try:
    (loss / accum).backward()          # 必然失败
except:
    (torch.zeros(1) / accum).backward()  # 也失败

# 新：torch.where 保留计算图连接 → FSDP hook 正常触发 → NCCL sync 正常
loss = torch.where(
    torch.isnan(loss) | torch.isinf(loss),
    torch.zeros_like(loss),    # 值=0，此处梯度被截断
    loss,                       # 值不变，梯度正常流通
)
(loss / accum).backward()       # 始终成功
```

### 效果
- 即使 loss 是 NaN/Inf，backward 梯度为 0 且 FSDP NCCL sync 正常完成
- 不会再有「NaN → backward 失败 → NCCL 死锁」的连锁崩溃
- 实际训练精度不受影响（NaN step 梯度为 0，等价于跳过该样本）

### 笔记同步
- `SFT.md` §8.11/8.12 — 重写为 v2 修复描述（float32 + torch.where）
- `LoRA.md` §6.7/6.8 — 同步更新为 v2 方案

## 2026-05-03 (v1)：NCCL IB 超时修复 + 重试机制增强

### 问题

NCCL `_ALLGATHER_BASE` / `_REDUCE_SCATTER_BASE` timeout，训练挂死在同一个 data sample（72B 模型 step 261 确定性地挂死；7B 模型也间歇触发）。根本原因是 InfiniBand 通信不稳定，而非数据样本问题。

### 修改文件

#### 1. [train/OpenFundus_SFT_zyz.magnus](train/OpenFundus_SFT_zyz.magnus) — SFT 全参微调蓝图

**NCCL 环境变量（shell 层，entry_command）：**
```python
# 旧
export TORCH_NCCL_HEARTBEAT_TIMEOUT_SEC=1800
export NCCL_DEBUG=WARN
export TORCH_NCCL_AVOID_RECORD_STREAMS=1
export NCCL_P2P_DISABLE=1
export NCCL_IB_TIMEOUT=22

# 新
export TORCH_NCCL_HEARTBEAT_TIMEOUT_SEC=600     # 超时从 30min → 10min（失败更快重试）
export NCCL_DEBUG=INFO                           # WARN → INFO（便于诊断）
export TORCH_NCCL_AVOID_RECORD_STREAMS=1
export NCCL_P2P_DISABLE=1
export NCCL_IB_TIMEOUT=22
export NCCL_IB_DISABLE=1                          # [新增] 禁用 IB，强制走 TCP
export NCCL_SOCKET_IFNAME=^docker,lo,virbr        # [新增] 排除虚拟网卡
export NCCL_NET_GDR_LEVEL=0                       # [新增] 禁用 GPUDirect RDMA
```

**NCCL 进程组超时（Python 层）：**
```python
# 旧
dist.init_process_group(backend="nccl", timeout=timedelta(seconds=1800))
# 新
dist.init_process_group(backend="nccl", timeout=timedelta(seconds=600))
```

#### 2. [train/LoRA_zyz.magnus](train/LoRA_zyz.magnus) — LoRA/QLoRA 微调蓝图

同上，相同的 NCCL 环境变量和时间设置。

#### 3. [train/monitor.py](train/monitor.py) — 训练监控

**修复 Windows GBK 编码崩溃：**
```python
# 旧
self._print(f"[{ts}] [{task_name}] ✅ 成功!")
self._print(f"[{ts}] [{task_name}] ❌ {status}")

# 新
self._print(f"[{ts}] [{task_name}] [OK] 成功!")
self._print(f"[{ts}] [{task_name}] [FAIL] {status}")
```

添加了 `_print()` 的 `UnicodeEncodeError` 兜底：
```python
def _print(self, *args, **kwargs) -> None:
    try:
        print(*args, **kwargs)
    except UnicodeEncodeError:
        safe_args = [str(a).encode('utf-8', errors='replace').decode('utf-8') for a in args]
        print(*safe_args, **kwargs)
```

#### 4. [train/submit_sft.py](train/submit_sft.py) — 提交脚本

未修改（仅读取配置，无逻辑变更）。

### 此前修改（prompt compression 前）

#### LoRA 蓝图新增功能

- **`_load_safetensors_state` 辅助函数**：从 safetensors 加载 state dict（支持分片：`adapter_model.safetensors.index.json` → `adapter_model.safetensors` → `model.safetensors.index.json` → `model.safetensors`）
- **`save_lora_checkpoint` 新增 optimizer/scheduler 保存**：`torch.save(optimizer.state_dict())` 和 `torch.save(scheduler.state_dict())`
- **完整 resume 逻辑**：处理 DDP (`module.` 前缀)、FSDP (`FULL_STATE_DICT` gather/load)、单 GPU 三种模式。加载 optimizer/scheduler/step 恢复。含 try/except 兜底重置
- **Shell 级 retry 循环**：`MAX_RETRIES=3` + `RETRY_SEED` 改变 `DistributedSampler` shuffle 种子 + `--resume_from_checkpoint checkpoint-latest`

### 同步副本

两个蓝图同时同步到 `PHY-LLM-Basic-Algorithm/zyz/` 目录。

### 当前训练状态（2026-05-03 15:00 更新）

两次提交记录：

| Job ID | Slurm ID | 状态 | 说明 |
|--------|----------|------|------|
| `fceb4f436b20fe64` | — | **Terminated** | 未分配 Slurm 资源，直接终止 |
| `bcd398927173cc99` | **9956** | **Pending** | 已进入 Slurm 队列，等待 GPU 分配 |

- 蓝图中已包含全部 NCCL 修复（`NCCL_IB_DISABLE=1`、超时 10min 等）
- LoRA 蓝图（`LoRA_zyz.magnus`）也已同步相同的 NCCL 配置
- 后台监控每 60s 轮询一次
- 模型版本：`Qwen2.5-Math-7B-Instruct-v1`（输出到 `...sft-v1` 目录，覆盖上一次失败运行的残留 checkpoint）
