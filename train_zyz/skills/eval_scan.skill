# eval_scan.skill — 容器内模型扫描补充

> **Level 2 — eval 子 skill。** eval.skill 已包含扫描逻辑核心内容，本文件补充实现细节。
> 父 skill: eval.skill

## 元信息
- **关联 .py**: eval_baseline.py(_SCAN_MODELS_PY内嵌脚本), config.py(parse_scan_json)
- **关联 skill**: eval.skill(父), eval_baseline.skill(调用方), magnus.skill→magnus_pattern.skill(sentinel JSON模式)
- **关联 .md**: 无专属文档
- **父 skill**: eval.skill

---

## 扫描路径扩展

- `/data/*/models/` — 所有用户的模型目录（glob 通配）
- `/data/magnus/models/` — Magnus 公共模型目录

## 参数量估算方式

从模型 `config.json` 的 `hidden_size`, `num_hidden_layers`, `num_attention_heads` 等字段估算。非精确值（`estimated_params_b` 单位为 B），用于 GPU 自动配置。

## scan_cache.json 缓存策略

缓存到 `train/json/scan_cache.json`。`--no-scan` 跳过扫描使用缓存，`--scan` 仅扫描不提交评估。

---

## 读完本 skill 后

- 使用扫描结果的评估流程 → `eval_baseline.skill`
- sentinel JSON 模式原理 → `magnus.skill` → `magnus_pattern.skill`
- config.parse_scan_json 实现 → `config.skill`
